1. Given a string, count the frequency of each character while ignoring spaces and letter case.
2. Given a string, find the first non-repeating character.
3. Given a string, return the character that appears the maximum number of times.
4. Given two strings, determine whether they are anagrams.
5. Given a string, remove duplicate characters while preserving their first occurrence.
6. Given a list of integers, remove duplicate elements while preserving the original order.
7. Given an array of integers, move all zeroes to the end while maintaining the order of the remaining elements.
8. Given a list of integers, find the second largest distinct element.
9. Given two lists, find the common elements without duplicates.
10. Given two sorted lists, merge them into a single sorted list without using built-in sorting functions.
11. Given a paragraph, count the frequency of every word using a dictionary.
12. Given a dictionary of employee names and salaries, find the employee with the highest salary.
13. Given two dictionaries, merge them. If a key exists in both dictionaries, add their values.
14. Given a dictionary, invert its keys and values.
15. Given a list of employee records (Name, Department), group employees by department using a dictionary.
16. Implement the Two Sum problem using a dictionary and return the indices of the two numbers whose sum equals the target.
17. Count the frequency of characters using `collections.Counter`.
18. Find the top three most frequent words in a paragraph using `Counter`.
19. Sort a list of employee records based on salary using a lambda function.
20. Implement a queue using `collections.deque` with enqueue, dequeue, and peek operations.
